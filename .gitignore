node_modules/
.turbo/
dist/
.next/
out/
coverage/
.env
.env.local
.env.*.local
*.log
*.tsbuildinfo
.DS_Store
.supabase/
supabase/.branches/
supabase/.temp/

.claude/settings.local.json
.claude/scheduled_tasks.lock

# Client-provided assets and local Supabase Studio scratch — not product
# source, not needed to build/deploy, and alianza-lima-sullana/ has real
# client photos that shouldn't leave this machine via a git remote.
/alianza-lima-sullana/
/front/
packages/db/supabase/snippets/

# Vercel CLI: guarda el id del proyecto/organización al enlazar.
.vercel

# Modelos de whisper.cpp (cientos de MB) — se descargan una vez a mano, ver
# apps/video-editor/README.md. WHISPER_MODEL_PATH apunta a uno de estos.
apps/video-editor/models/

# LUTs de cámara (del fabricante, no de este repo) — un preset las referencia
# por ruta absoluta en correccionColor.lutPath.
apps/video-editor/config/luts/

# Binario nativo de DeepFilterNet (~27 MB, descargado de GitHub releases) —
# ver DEEP_FILTER_BIN_PATH / apps/video-editor/README.md.
apps/video-editor/bin/
